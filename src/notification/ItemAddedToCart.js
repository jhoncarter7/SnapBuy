// import { useSelector } from "react-redux";
// import { useEffect, useRef } from "react";
// import { useDispatch } from "react-redux";
// import { notificationAction } from "../data/notification-slice";
// import classes from "./ItemAddedToCart.module.css";

const ItemAddedToCart = () => {
//   const isdatafetch = useSelector((state) => state.Popup.notificaion.isdatafetch);
//     const dispatch = useDispatch()
// const specialClass = useRef("")

// console.log(isdatafetch)
//   useEffect(() => {
//     if (isdatafetch) {
//       const timeId = setTimeout(() => {
//         // After 3 seconds set the show value to false
//         specialClass.current = `${classes.notificationActive}`;
//       }, 1000);

//       return () => {
//         clearTimeout(timeId);

//         if(!isdatafetch){
//             specialClass.current = "";
//         }
//       };
//     }



//   }, [isdatafetch, dispatch]);


//   let notifyme = `${classes.notification} ${specialClass.current}`;

  return (
    <div >
      <div></div>
    </div>
  );
};

export default ItemAddedToCart;
